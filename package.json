{
  "name": "parse-form-conditions",
  "version": "0.0.4",
  "description": "Repository for parsing formConditions",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "mocha --compilers js:babel-core/register --recursive",
    "posttest": "eslint --ext .js ./src ./test",
    "build": "babel --presets es2015 -d dist/ src",
    "eslint": "eslint --ext .js ./src ./test",
    "publish": "npm publish || true"
  },
  "author": "twocpbahmni@thoughtworks.com",
  "license": "GPL-3.0",
  "dependencies": {
    "esprima": "^4.0.1",
    "estraverse": "^4.2.0",
    "request": "^2.88.0",
    "sinon": "^7.2.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^6.2.2",
    "mocha": "^5.2.0"
  }
}
